* método do perceptron falta passar o dtype (vai considerar float32)

* generate_instances: dtype: np.int8 (fixo). precisa mudar de acordo com o parâmetro de entrada

* noise (linha 172): 128 está fixo. precisa seguir o parâmetro latent de entrada

* linha 208: cGAN treinada com x_train e y_train
* linha 225 (e outras / classificador): treinar com x_test, y_test
NOTA: não é o ideal, mas deixar assim por hora

* linhas 400, 401 (argparse de múltiplos valores): deveria ser array (não int)

* há problemas com a cGAN que precisam ser resolvidos

* hipótese / suspeita: cGAN original trabalha bem para dados balanceados
- solução 1 (eventual e objetiva): trabalhar com datasets balanceados
- solução 2: ajustar o código original para funcionar bem com datasets desbalanceados

